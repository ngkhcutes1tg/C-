#include <iostream>
#include <algorithm>

int a[100];

int main() {
    int n, k;
    std::cin >> n;
    for(int i = 0; i < n; i++)
        std::cin >> a[i];
    std::cin >> k;
    // for(int i = 0; i < n; i++)
    //     if(a[i] == k) {
    //         std::cout << i << "\n";
    //         break;
    //     }
}